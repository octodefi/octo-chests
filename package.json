{
  "name": "octochest",
  "version": "1.0.0",
  "description": "🧰 OctoChest - ERC721 NFT project with Hardhat + Foundry + IPFS",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat compile && hardhat run scripts/deploy.ts --network $NETWORK",
    "verify": "npx hardhat ignition verify chain-$CHAIN_ID",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-foundry": "^1.1.4",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.24.3"
  }
}
